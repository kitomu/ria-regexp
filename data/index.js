// Индексовый файл для сбора всех данный 

const lang = require('./lang');
const obl = require('./obl');
const city = require('./city');
const coast = require('./coast');
const obj_class = require('./obj_class');
const obj_type = require('./obj_type');
const room_amount = require('./room_amount');
console.log(lang)
module.exports = 
{
  lang,
  obl,
  city,
  coast,
  obj_class,
  obj_type,
  room_amount
}